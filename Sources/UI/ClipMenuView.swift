import SwiftUI

/// Root content of the status-bar MenuBarExtra.
///
/// TODO: Phase 2 — populate with ClipItem rows (history), SnippetSection,
///       ActionSection, and a separator + Preferences / Quit items.
///       Mirror the menu hierarchy from legacy/Source/MenuController.m
///       -buildClipMenu.
struct ClipMenuView: View {
    var body: some View {
        // TODO: Phase 2
        Text("ClipMenu")
    }
}
