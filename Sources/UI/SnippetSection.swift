import SwiftUI

/// Renders enabled snippet folders and their snippets as menu submenus.
///
/// TODO: Phase 2 — query SnippetService for enabled folders sorted by
///       sortIndex, build nested Menu views, call SnippetService.paste()
///       on selection.
struct SnippetSection: View {
    var body: some View {
        // TODO: Phase 2
        EmptyView()
    }
}
