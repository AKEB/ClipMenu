import SwiftUI

/// TODO: Phase 2 — inline preview length, icon style, number of items shown.
///       Reference: legacy/English.lproj/Preferences.strings (key "Menu").
struct MenuPrefsView: View {
    var body: some View {
        Form {
            Text("Menu preferences — TODO Phase 2")
        }
        .padding()
    }
}
