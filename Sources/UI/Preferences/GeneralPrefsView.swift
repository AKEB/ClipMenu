import SwiftUI

/// TODO: Phase 2 — implement using ClipMenuSettings @AppStorage properties.
///       Fields: login item toggle, max history size, app exclusion list.
///       Reference: legacy/English.lproj/Preferences.strings (key "General").
struct GeneralPrefsView: View {
    var body: some View {
        // TODO: Phase 2
        Form {
            Text("General preferences — TODO Phase 2")
        }
        .padding()
    }
}
