import SwiftUI

/// TODO: Phase 2 — action tree editor (enable/disable, reorder, add/remove).
///       Reference: legacy/Source/ActionNodeController.{h,m} and
///                  legacy/English.lproj/Preferences.strings (key "Actions").
struct ActionsPrefsView: View {
    var body: some View {
        Form {
            Text("Actions preferences — TODO Phase 2")
        }
        .padding()
    }
}
