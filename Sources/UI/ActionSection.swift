import SwiftUI

/// Renders the action tree as a menu submenu, filtered to actions applicable
/// to the most-recent clipboard entry.
///
/// TODO: Phase 2 — query ActionService.availableActions, build Menu hierarchy
///       respecting folder nesting, call ActionService.perform on selection.
struct ActionSection: View {
    var body: some View {
        // TODO: Phase 2
        EmptyView()
    }
}
